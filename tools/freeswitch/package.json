{
  "name": "@voxecho/freeswitch",
  "version": "0.1.0",
  "private": true,
  "description": "Script post-enregistrement FreeSWITCH — dépose wav+json au contrat §3 (CLAUDE.md §7, S5)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint test --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@voxecho/shared": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.2.7"
  }
}
